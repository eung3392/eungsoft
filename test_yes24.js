const fs = require("fs"); const html = fs.readFileSync(process.env.TEMP + "/book5.html", "utf8"); const titleMatch = html.match(/<title>(.*?)</title>/); const pubMatch = html.match(/class="(?:gd_pub|gd_maker)".*?>(.*?)<\/a>/) || html.match(/<span class="gd_pub">\s*<a[^>]*>([^<]+)<\/a>/); const dateMatch = html.match(/class="gd_date">([^<]+)<\/span>/); const imgMatch = html.match(/property="og:image" content="([^"]+)"/); const descMatch = html.match(/<textarea[^>]*class="txtContentText"[^>]*>([\s\S]*?)<\/textarea>/); console.log("TITLE:", titleMatch ? titleMatch[1].trim() : "N/A"); console.log("PUB:", pubMatch ? pubMatch[1].trim() : "N/A"); console.log("DATE:", dateMatch ? dateMatch[1].trim() : "N/A"); console.log("IMG:", imgMatch ? imgMatch[1].trim() : "N/A"); console.log("DESC:", descMatch ? descMatch[1].trim() : "N/A");
